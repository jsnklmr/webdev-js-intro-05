// Prevent us from attempting to use variables
// that are not declared
"use strict"
/*
let numbers = [1, 2, 3, 4, 5];
console.log(numbers);

// Create an array with different data types and log it
let mixedArray = [42, "hello", true, null, undefined, { key: "value" }, [1, 2, 3]];
console.log(mixedArray);

// Log the typeof an array
console.log(typeof numbers); // Returns "object"
console.log(Array.isArray(numbers)); // Returns true

// Remove an item from the array
myArray.pop(); // Removes last item
console.log(numbers);

// Add an item to the array
myArray.push("new item");
console.log(numbers);

// Change the value of an element
myArray[0] = "Updated Value";
console.log(numbers);

// Edge case: Accessing an index that doesn't exist
console.log(numbers[10]); // Returns undefined

// Edge case: Assigning a value to an out-of-bounds index
myArray[10] = "Out of bounds";
console.log(numbers);
console.log(numbers.length);
*/